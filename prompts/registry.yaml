# Prompt registry mapping invoice prompt versions to prompt files and schema contracts.

invoices:
  v1:
    prompt: prompts/invoices/v1/system.md
    schema: schemas/invoice_v1.json
    schema_version: v1
  v2:
    prompt: prompts/invoices/v2/system.md
    schema: schemas/invoice_v2.json
    schema_version: v2
